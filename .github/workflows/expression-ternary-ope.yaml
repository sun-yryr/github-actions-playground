name: expression ternary operator
on:
  workflow_dispatch:
    inputs:
      input1:
        type: string
        required: true

env:
  sample: |
    ${{
      inputs.input1 == 'allow' &&
        'OK' ||
        'NG'
    }}
  sample2: sample

jobs:
  sample:
    runs-on: ubuntu-latest
    steps:
      -
        run: echo ${{ env.sample }}
