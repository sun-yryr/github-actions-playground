name: example with space

on:
  push:
    branches:
      - "master"
  workflow_dispatch:
    inputs:
      notify_type:
        description: a
        required: true
        type: choice
        options: [notice, start]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      -
        run: echo \
          ${{ github.event.inputs.notify_type }}
